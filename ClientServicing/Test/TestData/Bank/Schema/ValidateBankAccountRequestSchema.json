{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "ValidateBankAccountRequest",
    "type": "object",
    "properties": {
        "idNumber": {
            "type": [ "string", "null" ],
            "description": "Identity number of the account holder (nullable)."
        },
        "bankAccountHolderInitials": {
            "type": [ "string", "null" ],
            "description": "Initials of the bank account holder (nullable)."
        },
        "surname": {
            "type": [ "string", "null" ],
            "description": "Surname of the account holder (nullable)."
        },
        "bankName": {
            "type": [ "string", "null" ],
            "description": "Name of the bank (nullable)."
        },
        "bankAccountNumber": {
            "type": [ "string", "null" ],
            "description": "Bank account number (nullable)."
        },
        "bankBranchCode": {
            "type": [ "string", "null" ],
            "description": "Bank branch code (nullable)."
        },
        "bankAccountType": {
            "type": [ "string", "null" ],
            "description": "Type of bank account (nullable)."
        }
    },
    "additionalProperties": false
}

